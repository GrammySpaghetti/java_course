import java.util.ArrayList;
import java.util.Arrays;


public class BasicTest{
    public static void main(String[] args){
        Basic first = new Basic();
        // first.print255();
        // first.printOdd();
        // first.printSum();
        // int[] arr = {3,5,7,9,13};
        // first.iterateArray(arr);
        // ArrayList<Integer> arr1 = new ArrayList<Integer>(Arrays.asList(-1, -2, -10,-19, -20));
        // first.findMax(arr1);
        // ArrayList<Integer> arr2 = new ArrayList<Integer>(Arrays.asList(1, 2, 10, 9, 20));
        // first.findAverage(arr2);
        // first.arrayOdd();
        // ArrayList<Integer> arr3 = new ArrayList<Integer>(Arrays.asList(-11, 2, 10, -9, 20));
        // int y = 1;
        // first.greaterY(arr3, y);
        // first.squareValues(arr3);
        int[] arr4 = {1, 2, 3, 4, 5};
        // first.maxMinAvg(arr4);
        first.shiftingValues(arr4);
    }
}